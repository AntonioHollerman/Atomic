using UnityEngine;

public class StatusEffect : Saveable
{
    // Status effect properties
    public string effectName;
    public int effectId;
    public float duration;

    // Constructor
    public StatusEffect(string effectName, int effectId, float duration)
    {
        this.effectName = effectName;
        this.effectId = effectId;
        this.duration = duration;
    }

    // Buff method to apply a beneficial effect
    public void Buff()
    {
        // Implement your logic to apply a beneficial effect here
    
    }

    // Debuff method to apply a detrimental effect
    public void Debuff()
    {
        // Implement your logic to apply a detrimental effect here
   
    }

    // Override the WriteSave method to save status effect data
    public override void WriteSave()
    {
        // Implement your logic to save status effect data here

    }

    // Override the SetSave method to set the save version ID for the status effect
    public override void SetSave(int verId)
    {
        // Implement your logic to set the save version ID for the status effect here
    }
}
